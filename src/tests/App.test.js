import { render, screen } from '@testing-library/react';
import App from '../App';

//Test to check if the App class works.
test ('AppTest',
                () => {
                 const className = App();
                  expect(className).test;
                });
